apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "my-mongodb.fullname" . }}-test-connection"
  labels:
    {{- include "my-mongodb.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "my-mongodb.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
