apiVersion: v1
kind: Service
metadata:
  name: {{ include "dynamicprovisioning.fullname" . }}
  labels:
    {{- include "dynamicprovisioning.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dynamicprovisioning.selectorLabels" . | nindent 4 }}
